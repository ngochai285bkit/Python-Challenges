# Update program 027 so that it will display the anwser to two decimal places

num = float(input("Enter a number with lots of decimal places: "))
answer = num * 2
print(answer)
print(round(answer, 2))
